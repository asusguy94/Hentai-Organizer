import { Grid, Typography } from '@mui/material'

const TestPage = () => (
	<Grid item id='test-page' className='text-center'>
		<Typography variant='h4'>Test Page</Typography>
		<Typography variant='h5'>A page without any logic</Typography>
	</Grid>
)

export default TestPage
